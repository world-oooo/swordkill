# 소울나이트 클론 — 게임 설명서

> 소울나이트의 **장르·구도·시스템**만 참고한 **오리지널 게임**.
> 원본 에셋은 복제하지 않으며, 아트는 직접 제작/AI 생성한 오리지널을 사용한다.

- **엔진**: Godot 4.6.3 (GDScript)
- **장르**: 2D 탑다운 로그라이크 (근접 검 전용)
- **프로젝트 경로**: `C:\Users\world\Desktop\찐찐찐막 대회용\soul-knight-clone\`
- **실행**: Godot로 `project.godot` 열기 → **F5** → 로비에서 "게임 시작 ▶"

---

## 1. 조작

| 입력 | 동작 |
|------|------|
| **WASD** | 이동 (왼쪽/오른쪽 키 방향으로 캐릭터가 바라봄) |
| (자동) | 검은 가장 가까운 적을 **자동 조준 + 자동 공격**. 적을 칠 땐 그 적 쪽을 바라봄 |
| **Space** | 스킬: 양손잡이 (잠시 더 빠르게 베기, 처치 시 지속 연장) |
| **Tab** | 인벤토리 열기/닫기 (열면 일시정지) |
| **R** | (사망 화면 등) 재시작 |

> 총기는 없다. 무기는 **검 전용**이며 자동 공격이다.

---

## 2. 게임 흐름

```
로비(시작) → 인게임(연결된 방들을 이동) → 보스 처치 → 결과(클리어/사망) → 로비
```

- **연결형 방 그래프(소울나이트식)**: 격자 위에 방 8개가 상하좌우 **문(통로)** 으로 연결된다.
- **시작 거점**에서 출발해 인접 방으로 자유롭게 **왕복** 가능.
- **전투방/보스방**은 진입 시 **문이 잠기고**(전투 중 못 나감), 적을 전멸시키면 **문이 열린다**.
- 시작에서 가장 먼 방이 **보스방**, 그 외 중 하나는 **상점**, 하나는 **보물방**, 나머지는 전투방.
- **보스를 처치하면 클리어(승리)** 로 런이 끝난다.

### 방 종류
| 종류 | 잠금 | 내용 | 미니맵 색 |
|------|------|------|-----------|
| 시작(거점) | X | 적 없음 | 파랑 |
| 전투 | O | 적 웨이브 | 회색(클리어=초록) |
| 정예 | O | 적은 수·강한 적 | 주황 |
| 보물 | X | 코인·젬 보상 | 노랑 |
| 상점 | X | 강화 구매 | 청록 |
| 보스 | O | 보스 | 빨강 |

> 방을 나갔다 와도 **바닥의 아이템·상점 좌판 상태가 유지**된다(저장/복원).

---

## 3. 캐릭터 — 기사(Knight)

| 항목 | 값 |
|------|----|
| HP | 7 |
| 실드 | 6 |
| 에너지 | 200 |
| 시작 무기 | 검 (자동 공격) |

- **패시브**: 실드가 깨지는 순간 **2초 피해 면역**.
- **스킬(Space) 양손잡이**: 6초간 더 빠르게 베기, 활성 중 적 처치 시 지속 연장(최대 12초), 에너지 60 소모.
- **이동 애니메이션**: 걸을 때 통통 튐(홉)+스쿼시, 멈추면 숨쉬기, 바라보는 방향으로 좌우 반전. 스프라이트는 4프레임 순환.

---

## 4. 무기 — 검 6종 (인벤토리)

처음부터 **6자루 모두 인벤토리에 보유**하며, **Tab 인벤토리에서 장착/해제**한다.
(같은 검을 다시 누르면 해제 = 맨손. 조합 시스템은 없음.)

| 키 | 이름 | 데미지 | 연사 | 사거리 | 특징 |
|----|------|--------|------|--------|------|
| wood | 나무 검 | 1 | 2.4 | 90 | 시작 무기 |
| steel | 강철 검 | 2 | 2.6 | 96 | |
| light | 빛 검 | 2 | 3.0 | 96 | 빠름 |
| rune | 룬 검 | 3 | 2.6 | 102 | **2프레임 애니메이션**(룬이 튀어나옴) |
| flame | 화염 검 | 3 | 2.2 | 106 | |
| radiant | 성검 | 4 | 2.8 | 112 | 최강 |

- 검마다 **검기(slash) 색**이 다르다(화염=주황, 룬=금색, 성검=밝은 금 등).
- 휘두를 때 **부채꼴 검기**가 베는 방향으로 쓸고 지나가며 사라진다(모든 검).

---

## 5. 전투 손맛 (타격감)

- **데미지 숫자**: 적을 칠 때 데미지가 위로 떠오르며 사라진다.
- **크리티컬**: 타격당 **3% 확률**, 데미지 **1.5배**. 크리티컬은 **더 크고 노란 숫자 + `!`**.
- **콤보**: 적을 칠 때마다 누적, 화면 중앙 위에 `N COMBO` 표시. 그 **위에 남은시간 바(―)** 가 줄어들고 **3초간 안 때리면 초기화**.
- **빨간 피격 플래시**: 맞은 적이 잠깐 빨갛게 번쩍인다.
- **넉백**: 맞은 적이 살짝 밀려난다.
- **화면 흔들림**: 적을 칠 때(5)·보스 칠 때(6)·플레이어가 맞을 때(8) 카메라가 흔들린다.
- **카메라 줌(1.6)**: 시점을 플레이어 주변에 집중해 몰입감을 높임.

---

## 6. 적과 보스

- **일반 적(스카프 도적)**: 근접 돌진형 / 원거리 사격형 두 행동. 층·방에 따라 체력·수 증가. 처치 시 코인 드랍. (스프라이트 6프레임)
- **보스(로봇 골렘)**: 전용 HP 바, 플레이어 추적 + 주기적 **원형 탄막**. 처치 시 젬 드랍 + 클리어. (스프라이트 6프레임)
- 모든 적은 같은 색으로 통일(피격 시에만 빨갛게).

---

## 7. 상점 / 재화

- **코인**: 적 처치·보물에서 획득. **젬**: 보스·보물에서 획득. HUD·인벤토리에 표시.
- **상점방** 좌판(닿으면 구매, 부족하면 가격표 깜빡):
  - 최대 HP +1 (젬 2)
  - 최대 실드 +1 (젬 2)
  - 최대 에너지 +20 (코인 25)

---

## 8. UI

- **좌상단**: HP(빨강)/실드(파랑)/에너지(노랑) 바 + 무기명 (크게)
- **우상단**: 코인·젬, 층 표시, **방 그래프 미니맵**(현재 방 강조), 타이머 (크게)
- **중앙 위**: 콤보 + 남은시간 바, 무기 획득 팝업, 보스 HP 바
- **우하단**: 스킬 아이콘 [Space]
- **인벤토리(Tab)**: 전체화면. 좌측 **장비 슬롯 4칸(투구/갑옷/장갑/신발 — 준비 중)**, 우측 **검/아이템 격자(6×4)**.
- **로비/결과 화면**: 크게 표시(승리=🏆 / 사망=💀, 도달·시간·처치·코인·젬 요약).

---

## 9. 프로젝트 구조

```
soul-knight-clone/
├── project.godot              # 메인씬=Lobby, 오토로드, Nearest 필터, 입력맵
├── 설명.md                    # (이 문서)
├── scripts/
│   ├── game_manager.gd        # (오토로드) 맵 그래프/진행/재화/콤보/보스/승리 허브
│   ├── weapon_db.gd           # (오토로드) 검 6종 데이터
│   ├── main.gd                # 인게임 루트: 방 로드/문 이동/종료
│   ├── room.gd                # 방 생성(적/보물/상점/보스)·문 잠금·아이템 저장/복원
│   ├── player.gd              # 이동/자동공격/방향/피해(실드패시브)/애니메이션/화면흔들림
│   ├── characters/knight.gd   # 기사 스탯 + 양손잡이 스킬
│   ├── weapon.gd              # 검: 자동 베기·크리·데미지숫자·검기·룬 애니메이션
│   ├── enemy.gd / boss.gd     # 적/보스(빨간 플래시·넉백)
│   ├── slash_effect.gd        # 검기(부채꼴)
│   ├── damage_number.gd       # 데미지 숫자
│   ├── portal.gd              # (구버전 포탈 — 현재 문 시스템 사용)
│   ├── hud.gd / minimap_view.gd
│   ├── inventory.gd           # Tab 전체화면 인벤토리(장착/해제)
│   ├── shop_item.gd
│   ├── lobby.gd / result.gd
│   └── pickups/{coin,gem}_pickup.gd
├── scenes/
│   ├── Lobby.tscn / Main.tscn / Result.tscn / Inventory.tscn / ShopItem.tscn / DamageNumber.tscn
│   ├── player/Player.tscn
│   ├── rooms/Room.tscn
│   ├── enemies/Enemy.tscn / Boss.tscn
│   ├── weapons/Weapon.tscn / Bullet.tscn / EnemyBullet.tscn / SlashEffect.tscn
│   ├── ui/HUD.tscn
│   └── pickups/{Coin,Gem}Pickup.tscn
└── assets/
    ├── player/  player_sheet.png (+원본, _process_sheet.py)
    ├── enemies/ enemy_sheet.png, boss_sheet.png (+원본, _extract.py)
    ├── sword/   sword_{wood,steel,light,rune,flame,radiant}.png (+원본, _process.py)
    └── tools/   process_sprite.py (범용 스프라이트 가공기)
```

### 핵심 설계 메모
- **GameManager(오토로드)** 가 맵·진행·재화·콤보의 허브. 적/픽업이 여기로 보고.
- **HUD는 시그널만 구독**(역참조 없음). 방 전환은 씬 리로드가 아니라 World 안 방만 교체(Player/HUD 유지).
- **충돌 레이어**: 1 플레이어 / 2 적 / 4 벽 / 8 플레이어탄(현재 미사용) / 16 적탄.
- 물리 콜백 중 픽업(Area2D) 생성은 `add_child.call_deferred`로 처리(에러 방지).

---

## 10. 스프라이트 교체 방법

직접 만든(또는 CC0/프리) 이미지를 게임용 시트로 가공해 넣는다. 범용 도구: `assets/tools/process_sprite.py`

1. 이미지를 해당 폴더에 넣는다 (캐릭터=`assets/player/`, 적/보스=`assets/enemies/`, 검=`assets/sword/`).
2. 가공: `python ../tools/process_sprite.py <입력.png> <출력.png> --frames N` (흰/단색 배경 제거 + N등분).
3. 출력에 찍힌 **cell 크기·프레임 수**를 확인 → 해당 씬의 `Sprite2D`(`hframes`, `scale`, `position`)에 연결.

- 캐릭터/적: 정면 또는 오른쪽 향함. 검: **오른쪽(→) 향하게, 손잡이 왼쪽**.

---

## 11. 아직 안 만든 것 (다음 후보)

- 장비(방어구) 실제 착용 기능 — 인벤토리 좌측 장비 슬롯에 연결 예정
- 젬 영구(런 간) 강화, 상점 아이템 확장
- 다른 캐릭터, 기사 나머지 스킬
- 사운드/음악, 타이틀 연출
- 웹(HTML5) 내보내기 → 배포

---

## 12. 검증 방법 (개발용)

```
# 임포트/파싱·씬 참조 에러 체크
Godot_v4.6.3 --headless --editor --quit --path <project>
# 런타임 스모크
Godot_v4.6.3 --headless --path <project> scenes/Main.tscn --quit-after 120
```
